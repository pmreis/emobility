name: Simple scrape

on:
  push:
  workflow_dispatch:
  schedule:
    - cron:  '0 1 * * *'

jobs:
  scheduled:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Check out this repo
      uses: actions/checkout@v4
      
    - name: Setup python
      uses: actions/setup-python@v5
      with:
         python-version: 3.9
         
    - name: Setup python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run simple scrape test
      run: |
        python ./src/scrape.py

    - name: Fetch data via curl
      run: |
        curl -k https://mobie.pt/documents/42032/106470/8602.csv/d8679fe8-51c0-00ce-97b7-37f8a930c861?t=1711969200415 > ./data/sources/mobie_tariffs.csv

    - name: Commit and push if it changed
      run: |
        git config user.name "Automated"
        git config user.email "actions@users.noreply.github.com"
        git add -A
        timestamp=$(date -u)
        git commit -m "Latest tariffs: ${timestamp}" || exit 0
        git push
